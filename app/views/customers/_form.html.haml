-# frozen_string_literal: true
= simple_form_for(@customer) do |f|
  = f.error_notification
  = f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present?

  .form-inputs
    = f.input :vip
    = f.error_span :vip
    = f.input :first_name
    = f.error_span :first_name
    = f.input :last_name
    = f.error_span :last_name
    = f.input :driving_directions, as: :text
    = f.error_span :driving_directions

    %input{ name: "val1" }
    %input{ name: "val1" }
    %input{ name: "val1" }
    %input{ name: "val1" }
    %input{ name: "val1" }
    %input{ name: "val1" }


  .form-actions
    = f.button :submit
